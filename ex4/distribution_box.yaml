# 这里的路径建议写绝对路径，或者相对于运行目录的路径
path: ./my_dataset  # 数据集根目录
train: images/train # 训练集图片 (相对于path)
val: images/val     # 验证集图片 (相对于path)

# 类别数量
nc: 1

# 类别名称 (根据 Slide 3 显示为 unclosed，对应“门未关闭”)
names:
  0: unclosed